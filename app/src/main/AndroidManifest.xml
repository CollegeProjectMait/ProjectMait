<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parasrawat2124.projectmait">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:theme="@style/ProjectMaitTheme">

        </activity>
        <activity android:name=".ParasRawatActivity"
            android:theme="@style/ProjectMaitTheme" />
        <activity android:name=".LoginActivity"
            android:theme="@style/ProjectMaitTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterUserActivity" android:theme="@style/ProjectMaitTheme">
        </activity>
    </application>

</manifest>